{
  "_comment": "esse arquivo é de configuração da base de dados",

  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "gostack_gobarber",
  "entities": ["./src/modules/**/infra/typeorm/entities/*.ts"],
  "migrations": ["./src/shared/infra/typeorm/migrations/*.ts"],
  "cli": {
    "_comment": "configurações para a interface de linha de comando do typeorm",
    "migrationsDir": "./src/shared/infra/typeorm/migrations"
  }
}
